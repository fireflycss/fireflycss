import { Config } from "../utils/data/config";
import { Utilities } from "../utils/data/utilities";
//todo proper type hinting for all user files
export function defineConfig(config: Config): Config {
  return config;
}
export function defineUtilities(utilities: Utilities): Utilities {
  return utilities;
}
