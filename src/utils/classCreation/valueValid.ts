export function isValueValid(
  input: string /*, utilityData: UtilityData*/
): boolean {
  //todo get faster ay to check if exists in list
  //if (dataTypes.indexOf("keyword") !== -1 && input.match(/^[a-z\-]+$/)) {
  //    return true;
  //}
  if (input) return true; //!Temp remove once complete
  return false;
}
